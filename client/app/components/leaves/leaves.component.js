import template from './leaves.html';
import controller from './leaves.controller';
import './leaves.scss';

let leavesComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default leavesComponent;
